@model List<E_Ticaret.Entities.Urun>

<table class="table table-sm bordered">
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Ad</td>
            <td>@item.Fiyat</td>
        </tr>
    }
</table>
